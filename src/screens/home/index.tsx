import { View } from 'react-native';
import Text from '@components/text';
import Icon from '@components/icon';

const Home = () => {
  return (
    <View>
      <Text>Home</Text>
      <Icon name="home" />
      <Icon name="quiz" />
    </View>
  );
};

export default Home;
